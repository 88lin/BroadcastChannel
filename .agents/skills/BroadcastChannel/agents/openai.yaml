interface:
  display_name: "BroadcastChannel"
  short_description: "Repo-specific patterns and workflows for BroadcastChannel"
  default_prompt: "Use the BroadcastChannel repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true